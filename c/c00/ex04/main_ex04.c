void    ft_is_negative(int n);

int	main(void)
{
	int	num;
/*
	printf("Plese enter an integer number(negative, postive or 0): ");
	scanf("%d", &num);
	ft_is_negative(num);
 */
    
    num = 0;
    ft_is_negative(num);
}
